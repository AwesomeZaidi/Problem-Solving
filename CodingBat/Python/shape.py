# Enter your code here. Read input from STDIN. Print output to STDOUT

class Shape:
    __init__(self, numberOfSides):
        self.shapeType = shapeType
        
    getArea():
        print("None")

class Circle:
    __init__(self, radius):
        self.radius = 4
    
    getArea(self):
        """
            Calculate circle area: pi * r^2
        """
        
        area = 3.14 * self.radius ** self.radius
        
        return area
        
class Triangle(Shape):
    __init__(self, height, base):
    super().__init__(self, numberOfSides)
        self.height = 4
        self.base = 5
    
    getArea(self):
        """
            Calculate triangle area: (b * h) / 2
        """
        
        area = (self.base * self.height) / 2
        
        return area

class Square(Shape):
    __init__(self, width, height):
    super().__init__(self, numberOfSides)
        self.width = 4;
        self.height = 5;
    
    getArea(self)
        """
            Calculate sqaure area: w * h
        """
        
        area = self.width * self.height
        
        return area
        
    
# (a + b) / 2 * h

def getLargestShape(Shape[] shapes)
"""
    Args: arr of shape objs
    return the Shape with the largest area
"""

    for shape in shapes:
        if shape.shapeType == "triangle"

        
        